spring:
  application:
    name: url-shortner
  datasource:
    url: jdbc:postgresql://localhost:55432/url_shortner_db
    username: postgres
    password: 123456789
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    # show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        # format_sql: true 
jwt:
  secret: 4qX/F6mBJqNVAzx1tJf79UxyVshLE4Ov959a1U9mXc3mbAMZ9KRosmvff/b4riHS
  expirationMs: 3600000 # 1 hour in milliseconds
logging:
  file:
    name: logs/url-shortner.log
  logback:
    rollingpolicy:
      max-file-size: 10MB
      max-history: 7
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
  level:
    root: INFO
    "[com.shortner.url_shortner, org.hibernate.SQL, org.springframework.security]": DEBUG 